#include <stdio.h>
#include <conio.h>

void main()
{
	//Print odd numbers till 20 using for loop.
	int count;
	for(count=1; count<=20; count=count+2)
	{
		printf ("\n %d",count);
	}

	getch();
}